# Claude Code — kept local, not committed
CLAUDE.md
.claude/

# Editor / IDE — local-only
.vscode/
*.code-workspace

# Local secrets (WiFi credentials etc.) — see shared/include/secrets.example.h
shared/include/secrets.h
