# Claude Code — kept local, not committed
CLAUDE.md
.claude/

# Editor / IDE — local-only
.vscode/
*.code-workspace

# Local secrets (WiFi credentials etc.) — see shared/include/secrets.example.h
shared/include/secrets.h

# Local Node.js config (your LAN IP etc.) — see shared/node/config.example.js
shared/node/config.js

# Node modules (per-folder; never committed)
node_modules/
